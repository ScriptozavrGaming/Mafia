<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="Scriptozavr.Mafia"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="19"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application android:label="@string/app_name" android:icon="@drawable/ic_launcher">
        <activity android:name=".Activities.Main"
                  android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".Activities.ChoosingRoles"
                android:label="@string/app_name"/>
        <activity android:name=".Activities.SetNicknames"
                  android:label="@string/app_name"/>
        <activity android:name=".Activities.MorningActions"
                  android:label="@string/app_name"/>
        <activity android:name=".Activities.VoteForElimination"
                  android:label="@string/app_name"/>
        <activity android:name=".Activities.DuelVoting"
                  android:label="@string/mafia"/>
        <activity android:name=".Activities.NightActions"
                  android:label="@string/app_name"/>
        <activity android:name=".Activities.DuelistsSpeech"
                  android:label="@string/app_name"/>
        <activity android:name=".Activities.LastMinute"
                  android:label="@string/app_name"/>
    </application>

</manifest>
